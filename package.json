{
    "name": "postcss-px2rem-ks",
    "version": "1.0.3",
    "description": "Based on postcss-px2rem added the exclude folder options.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "postcss": "^8.2.10",
        "px2rem": "^0.5.0"
    }
}
